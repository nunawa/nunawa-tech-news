---
import "../styles/global.css";

interface Props {
  title?: string;
  description: string;
}

const { title, description } = Astro.props;
const siteTitle = "Nunawa Tech News";
const fullTitle = title ? `${title} | ${siteTitle}` : siteTitle;
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <title>{fullTitle}</title>
    <meta name="description" content={description} />

    <!-- Atom Feed -->
    <link
      href="/atom.xml"
      type="application/atom+xml"
      rel="alternate"
      title={siteTitle}
    />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:site_name" content={siteTitle} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
  </head>
  <body class="min-h-screen bg-neutral-950 text-neutral-100">
    <header class="border-b border-neutral-800 bg-neutral-900">
      <nav
        class="container mx-auto flex max-w-4xl items-center justify-between px-4 py-4"
      >
        <a
          href="/"
          class="text-xl font-bold text-neutral-300 transition-colors hover:text-white"
        >
          {siteTitle}
        </a>
        <div class="flex gap-2 sm:gap-6">
          <a
            href="/"
            class="rounded px-2 py-1 text-neutral-300 transition-colors hover:text-white focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-neutral-900 focus:outline-none"
          >
            Home
          </a>
          <a
            href="/about"
            class="rounded px-2 py-1 text-neutral-300 transition-colors hover:text-white focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-neutral-900 focus:outline-none"
          >
            About
          </a>
          <a
            href="/atom.xml"
            class="rounded px-2 py-1 text-neutral-300 transition-colors hover:text-white focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-neutral-900 focus:outline-none"
          >
            RSS
          </a>
        </div>
      </nav>
    </header>

    <main class="container mx-auto mb-16 max-w-4xl px-4 py-8">
      <slot />
    </main>
  </body>
</html>
